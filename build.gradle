version "${versionPPR}.${versionBuild}${versionApi}"

apply plugin: 'java'
apply plugin: 'idea'

repositories {
    mavenCentral()
}

allprojects {
    group 'com.mn'
}
subprojects {
    dependencies {
        testCompile("junit:junit:4.11")
    }
}
