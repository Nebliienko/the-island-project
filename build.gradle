include gradle.properties
version "${versionPPR}.${versionBuild}${versionApi}"

allprojects {
    group 'com.mn'
}

apply plugin: 'java'
apply plugin: 'war'
apply plugin: 'idea'

repositories {
    mavenCentral()
}

dependencies {
    testCompile group: 'junit', name: 'junit', version: '4.11'
}
